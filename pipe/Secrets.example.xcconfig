// Template for pipe/Secrets.xcconfig (which is gitignored).
// Copy this to Secrets.xcconfig and fill in the value from AWS:
//   aws apigateway get-api-key --api-key 9kowamshy4 --include-value \
//     --profile personal --region us-west-2 --query value --output text
PIPE_LOGS_API_KEY =
